import {expect, test, describe, it} from 'vitest'

describe("Register CreateUserUseCase", () => {
    it ("Should hash user password upon registration", () => {})    
    it ("Should be able to create a new user", () => {

    })

    it ("Should not be able to create a new user with an existing email", () => {

    })

    it ("Should not be able to create a new user with an existing username", () => {})
})